import { useTranslation } from "react-i18next";

/** Marketing copy on homepage, quiz, and What is Palette Plotting. English-only. */
export function useMarketingTranslation() {
  return useTranslation("marketing");
}
