import { useTranslation } from "react-i18next";

/** Marketing copy on homepage and What is palette plotting. English-only. */
export function useMarketingTranslation() {
  return useTranslation("marketing");
}
